/**
 * NOTE
 * Module federation with ts-loader does not seem to work properly if the index is .ts
 * Getting 'Error: Shared module is not available for eager consumption' for react
 */

import('./bootstrap')
